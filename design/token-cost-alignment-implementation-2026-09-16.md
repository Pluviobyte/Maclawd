# Token、费用与工具自动发现实施记录

基于本轮最新上游核查：Vibe CLI fcf1c398 / npm0.10.31、app6feb8bce、Codex50d77959 / rust-v0.154.0、ccusage5e88263 / v20.0.20。字段与差异详见同日审计，价格以供应商官方公开合同为准，不复制第三方实现。

## 第一部分：可更新的官方价格与安全降级

- 同时下载公开OpenAI、Anthropic Markdown价表及OpenRouter目录，官方价优先，用户override保持最高优先级。每个供应商刷新失败保留它的上次成功快照，记录错误和最近成功时间；首次离线使用2026-09-16核对过的官方数值快照。
- 修正Haiku4.5，Sol采用官方标准价。保存显式Fast/Batch/Flex与长上下文价格。新模型有未知阈值时不猜；当前OpenAI家族>272K阈值来自官方模型页。
- 删除模型家族关键词兜底、未发布缓存价的通用倍率；明确免费为0，缺价仍未知。目录保留pricing.overrides。
- 24项价格/调度测试通过，覆盖阈值、不同档位、官方更新、源失败保留旧表、手工覆盖、免费/未知。
- 隔离临时目录真实GET验证成功：438个目录模型，OpenAI/Anthropic两份官方表均成功更新；Sol输入4、Haiku4.5输入1美元/百万。没有读取供应商凭据或上传模型清单。

费用继续按当前公开API价估算，不宣称实际订阅账单。后续部分负责把逐请求维度保留到聚合和界面。

## 第二部分：逐请求计价维度

- Codex在历史核对及实时续读状态中保留service_tier，priority按官方Fast别名处理；Claude读取usage.speed。模型本身保持原身份，档位不靠猜模型字符串。
- 扫描缓存21、聚合14；逐请求输入长度和服务档位保留到模型/项目单元的chargeGroups，精确输入长度保证未来阈值更新可重算，不用日累计量误判长上下文。原Token总量不变。
- 累计快照不能还原单次上下文时明确promptTokens=null；需要阈值而上下文未知的部分不计价。相同模型的已知请求仍计价，单独累计未计价Token。
- 保留5m/1h缓存写，输出不重复加推理。UI将“费用覆盖”改为“价格匹配”，设置明确官方+OpenRouter来源。
- 新增同日多短请求、单长请求、Fast/Flex、缓存序列化、冷/暖/追加、未知档位/上下文及Claude Fast TTL合同测试。当前工作区全量744项通过（包含此前未提交的独立测试；最终推送前再验证提交树）。

## 第三部分：动态工具发现

- 已安装、支持额度的来源在第一次成功返回前即可进入概览右上角选择器，按真实状态提示等待、未登录、读取失败或开关关闭；不伪造百分比。后台重新检查安装，已读取的历史额度仍保留。
- 用量型工具在产生第一条记录之前就进入统计筛选；仅支持用量、没有额度接口的工具不会生成虚假额度。Cursor、Grok、Kimi Code 的 capability 与实际采集器统一。
- 保留用户已有隐藏/排序偏好，新发现 ID 默认显示并追加。Swift 兼容旧 runtime。
- 31 项针对性测试通过，包括 HTTP 动态发现、地区应用身份、Swift 解码及排序合同；release Swift 编译通过。

## 第四部分：补齐本地来源与解析差异

- 独立实现 Qoder / Qoder CN 的 IDE SQLite 与 CLI JSONL、Cola Pi 会话和 Cindy / CindyGlobal 日账本，来源依据及最新 revision 见 missing-source-contracts-2026-09-16.md。均保留待真机验证状态。
- SQL 仅选择用量字段，活跃 WAL 变化触发替换快照，schema 失败保留旧结果。Qoder 积分不反推 Token；Cola 复制记录按复合身份去重；Cindy 排除原生 Claude，当用户配置扫描 Cindy 私有 Codex/Pi 路径时停用对应账本，避免相加。
- Trae CLI 微秒时间、主 span / failover 层、模型字段、推理输出和增量重选对齐当前 Vibe 行为。该产品具体字段仍缺独立官方/真机证明，文档明确限制。
- 现代 Pi 1h 缓存写保留；未知 TTL 阻止伪造 5m 费用。Cindy billing 尾标记可用于已核实的价格别名；日/任务累计量不用于判定逐请求长上下文。
- 日账本保存到单独日聚合，统计总量和日趋势可用，小时热图和最近24小时不混入；界面明确日粒度与缺失范围。聚合15、扫描缓存22。
- 42项解析/费用/分析测试通过，release Swift编译通过。全量753项中发现并更新1条与新自动发现语义冲突的旧 Kimi 测试；该测试仍验证未开启时绝不请求及关闭后清除真实额度。
